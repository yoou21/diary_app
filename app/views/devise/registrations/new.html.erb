<div style="max-width: 800px; margin: 50px auto; padding: 20px; border: 1px solid #ddd; border-radius: 8px;">
  <h1>ユーザー登録</h1>
  <p>以下のフォームに必要な情報を入力して、アカウントを作成してください。</p>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <table>
      <tr>
        <td><%= f.label :email, "メールアドレス" %></td>
        <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
      </tr>
      <tr>
        <td><%= f.label :password, "パスワード" %></td>
        <td>
          <% if @minimum_password_length %>
            <small>(パスワードは<%= @minimum_password_length %>文字以上です)</small>
          <% end %>
          <br>
          <%= f.password_field :password, autocomplete: "new-password", placeholder: t('devise.views.password.minimum_length') %>
        </td>
      </tr>
      <tr>
        <td><%= f.label :password_confirmation, "パスワード（確認用）" %></td>
        <td><%= f.password_field :password_confirmation, autocomplete: "new-password" %></td>
      </tr>
    </table>

    <div style="text-align: center; margin-top: 20px;">
      <%= f.submit "登録する", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
